// Top-level build file.
//
// The upstream buildscript{} block (AGP 3.3.2, jcenter()) is gone. Plugins are declared
// here with the plugins {} DSL and resolved through pluginManagement in settings.gradle;
// repositories for dependencies live in settings.gradle dependencyResolutionManagement.

plugins {
    id 'com.android.application' version '8.13.2' apply false
}
